# Шаблон для 8 задачи ЕГЭ по информатике

'''
Все пятибуквенные слова, составленные из букв Б, У, Р, А, Т, И, Н, О записаны в алфавитном порядке и пронумерованы.
Вот начало списка:
1. ААААА
2. ААААБ
3. ААААИ
4. ААААН
5. ААААО
6. ААААР
……
Определите, под каким номером в этом списке стоит последнее слово с нечётным номером, которое не начинается с гласных букв и при этом все буквы в слове различны.
Примечание. Слово – последовательность идущих подряд букв, не обязательно осмысленная.
'''

# 1 способ
from itertools import *

# цикл - сначала пишем продукт, в него буквы из которых составляем слово, а в repeat пишем длину нашего слова
# enumerate - функция для облегчения подсчета, аргумент 1, потому что отсчет начинаем с единицы
# ВНИМАНИЕ!!! вместо enumerate можно завести любую переменную-счетчик и использовать ее
for num, p in enumerate(product(sorted('БУРАТИНО'), repeat=5), 1):
    s = ''.join(p) # склеиваем наш набор букв в слово
    # Условие: номер нечетный, 1 буква - согласная, все буквы разные
    if num % 2 == 1:
        if s[0] not in 'УАИО':
            m = set() # создаем множество. Используем его главное свойство - элементы в множестве не повторяются
            # а значит, что если кол-во букв в множестве совпадает с кол-вом в строке, то все буквы разные
            for j in s:
                m.add(j)
            if len(m) == len(s):
                print(num) # последнее число в консоли и будет нашим ответом :)



# 2 способ
'''Второй способ заключается в использовании вложенных циклов, что при больших кол-вах букв
может вызвать путаницу. Крайне не советую этот способ, хотя в исключительно редких случаях его можно использовать,
например, если нужно чередовать гласные и согласные.
Приведу примерный вид такого решения:'''

s = sorted('БУРАТИНО')
num = 0
for s1 in s:
    for s2 in s:
        for s3 in s:
            for s4 in s:
                for s5 in s:
                    stroka = s1 + s2 + s3 + s4 + s5
                    num += 1
                    if num % 2 == 1:
                        if stroka[0] not in 'УАИО':
                            m1 = set()  # создаем множество. Используем его главное свойство - элементы в множестве не повторяются
                            # а значит, что если кол-во букв в множестве совпадает с кол-вом в строке, то все буквы разные
                            for j in stroka:
                                m1.add(j)
                            if len(m1) == len(stroka):
                                print(num)  # последнее число в консоли и будет нашим ответом :)


'''Как видите вложенность решения не внушает доверия, а при большом кол-ве букв это очень фатально
Однако такое решение тоже имеет место быть!'''